spring.application.name=backend

# Server Port Configuration
server.port=8080

# MySQL Connection Settings

# Default MySQL port is 3306, not 5432 (that was PostgreSQL's port).
spring.datasource.url=jdbc:mysql://localhost:3306/student_tracker?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.username= xolela
spring.datasource.password=@!St5408088


# JPA / Hibernate Configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate MySQL Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable OSIV warning
spring.jpa.open-in-view=false

# Security settings
spring.security.user.name=root
spring.security.user.password=xolela

logging.level.org.springframework.security=DEBUG